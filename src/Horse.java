public class Horse extends ChessPiece {
    Horse horse = new Horse();
    String symbol ="H";
    String color = "White";
    String getColor(){
        return getColor();
    }
    private String getSymbol() {
        return getSymbol();
    }
    private int canMoveToPosition(ChessBoard chessBoard, int line, int column, int toLine, int toColumn){
toLine = line + 3;
toColumn = column +2;
if (1< toLine && toLine > 8 ){
    return Integer.parseInt(null);
        }
        return line;
    }
    }

